#include <stdio.h>
int main()
{
    int N[10], a, n;
    scanf("%d", &n);
    for(a=0; a<=9; a++)
    {
        N[a]=s;
        printf("N[%d] = %d\n",a,n);
        n=n*2;
    }
    return 0;
}
